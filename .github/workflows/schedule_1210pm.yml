name: "12:10 PM - Late Students Afternoon (Mon-Fri)"

on:
  schedule:
    - cron: '25 4 * * 1-5'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  send:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
      - name: Run script
        run: python3 scripts/telegram_bot_1210pm.py
        env:
          TZ: "Asia/Phnom_Penh"
          TARGET_TIME: "12:10"
          BOT_TOKEN: ${{ secrets.BOT_TOKEN }}
      - name: Save counter
        run: |
          git config user.email "bot@dpss.school"
          git config user.name "DPSS Bot"
          git pull --rebase --autostash origin main
          git add counter.json
          git diff --staged --quiet || git commit -m "Update counter [skip ci]"
          git push
